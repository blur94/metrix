import withLayout from "@/layouts/appLayout"
import React from 'react'

function Conversations() {
  return (
    <div>index</div>
  )
}

export default withLayout(Conversations, "Conversations", "Conversations")
